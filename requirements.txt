# === Core dependencies ===
streamlit>=1.39
pandas>=2.2
numpy>=1.26

# === Excel I/O and export ===
openpyxl>=3.1.2
xlsxwriter>=3.2.0
xlrd==2.0.1   # for legacy .xls support only

# === Machine learning / semantic analysis ===
scikit-learn>=1.5.0
scipy>=1.13.0

# === OCR / PDF processing ===
Pillow>=10.3.0
pytesseract>=0.3.11
PyPDF2>=3.0.1
pdf2image>=1.17.0

# === Large dataset & analytics (Plan B: DuckDB + Parquet) ===
duckdb==1.1.3
pyarrow==16.1.0
fastparquet>=2024.5.0

# === System / optimization utilities ===
psutil>=5.9.8     # optional: for memory usage sidebar

# === Optional: for environments where poppler is missing ===
# (uncomment the next line if you deploy somewhere without native poppler)
# poppler-utils